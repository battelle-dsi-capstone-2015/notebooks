# Several functions to analyze LDA output:
# inputs are doc-term matrices, topic-alpha vector, vocabulary, 
# document dataframe with doc attributes

print(parent.frame(1)$ofile)
source('dirichlet.R')
x = parent.frame(2)
